# Nested Folder Storage Platform (Frontend)

## Features
- Upload ZIP with nested folders
- Explore folders recursively
- View PDFs and CSVs
- Rename, delete, upload files inside folders

## Setup
```bash
cd client
npm install
npm run dev
